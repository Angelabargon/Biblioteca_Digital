<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.biblioteca_digital.controladores.admin.ControladorUsuarioAdmin"
      spacing="20"
      style="-fx-background-color:#f6f2ec; -fx-padding:32;">

    <!-- TÍTULO + BOTÓN -->
    <HBox alignment="CENTER_LEFT" spacing="16">
        <Label text="Gestión de Usuarios"
               style="-fx-font-size:24px; -fx-font-weight:700; -fx-text-fill:#3a3a3a;"/>

        <Region HBox.hgrow="ALWAYS"/>

        <Button text="+  Nuevo Usuario"
                onAction="#abrirAgregarUsuario"
                style="
                    -fx-background-color:#8b4b2e;
                    -fx-text-fill:white;
                    -fx-font-weight:600;
                    -fx-font-size:14px;
                    -fx-padding:10 22;
                    -fx-background-radius:12;">
        </Button>
    </HBox>

    <!-- TABLA ESTILO FIGMA -->
    <TableView fx:id="tablaUsuarios"
               VBox.vgrow="ALWAYS"
               style="
                   -fx-background-color:white;
                   -fx-background-radius:14;
                   -fx-border-color:#e5e3dd;
                   -fx-border-radius:14;
                   -fx-table-cell-border-color: transparent;
                   -fx-padding:6;
                   -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.10), 14, 0, 0, 4);
                   -fx-fixed-cell-size:46;">

        <columns>

            <TableColumn fx:id="colUsuario" text="Nombre" prefWidth="240"
                         style="-fx-font-size:14px; -fx-font-weight:600;"/>

            <TableColumn fx:id="colCorreo" text="Email" prefWidth="280"
                         style="-fx-font-size:14px; -fx-font-weight:600;"/>

            <TableColumn fx:id="colRol" text="Rol" prefWidth="150"
                         style="-fx-font-size:14px; -fx-font-weight:600;"/>

            <TableColumn fx:id="colFecha" text="Fecha de Registro" prefWidth="180"
                         style="-fx-font-size:14px; -fx-font-weight:600;"/>

            <TableColumn fx:id="colAcciones" text="Acciones" prefWidth="130"
                         style="-fx-font-size:14px; -fx-font-weight:600;"/>

        </columns>

    </TableView>

</VBox>
