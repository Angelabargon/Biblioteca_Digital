package com.example.biblioteca_digital.modelos;

/**
 * Enumeración Rol que define los tipos de usuario disponibles en la aplicación.
 *
 * Cada rol tiene distintos permisos y funcionalidades a las que puede acceder
 * un usuario dentro del sistema.
 */
public enum Rol {
    usuario,admin,admin_principal
}
